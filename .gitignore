*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# Kotlin compiler daemon sessions
.kotlin

# Backup files
*.bak

# Native binding — built from sing-box by CI (scripts/build-libbox.sh), not committed
/app/libs/*.aar

# CI / native-build scratch
/.libbox-build/
/.go/
/.gocache/
/.android-sdk/

# AI assistant personal settings
.claude/

# Local Gradle daemon JVM config
gradle/gradle-daemon-jvm.properties

# Firebase App Distribution service-account credentials — never commit
/firebase-service-account.json

# Release signing keystore — never commit; CI gets it from a base64 secret
*.jks
*.keystore

# Font source archive (extracted into app/src/main/res/font)
/Google_Sans.zip
